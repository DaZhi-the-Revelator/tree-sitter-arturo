; Injection queries for embedded code blocks in Arturo
; Note: Arturo uses string literals for embedded code, not dedicated grammar nodes
; These injections are disabled as the grammar does not define code_block nodes
